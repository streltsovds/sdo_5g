let eslint_rule_vue_attributes_order_patch = require("./eslint-plugin-vue/lib/rules/attributes-order_patch");
let eslint_rule_vue_attributes_order = require("eslint-plugin-vue/lib/rules/attributes-order")

/**
 * Позволяет делать class первым атрибутом для удоства свёртывания структуры в PhpStorm
 *
 * Пример:
 *
 * ```
 * <div class="carousel-catalog__slider-block-icon">
 *   <div class="carousel-catalog__slider-block-icon__image">
 *     <div class="carousel-catalog__slider-block-icon__image-img"
 *          v-if="item.image && item.image !== ''"
 *          :style="{ backgroundImage: `url(${item.image})` }"
 *     ></div>
 * ```
 */
eslint_rule_vue_attributes_order_patch(eslint_rule_vue_attributes_order)
